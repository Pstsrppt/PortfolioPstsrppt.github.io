# Vibe Coding Explanation

**ชื่อ:** พงศธร ศิริพรหมพิทักษ์ (Pongsathorn Siriprompitak)
**สาขา:** วิทยาการคอมพิวเตอร์และนวัตกรรมการพัฒนาซอฟต์แวร์ มหาวิทยาลัยศรีปทุม
**Public URL:** https://pstsrppt.github.io/PortfolioPstsrppt.github.io/
**Source Code:** https://github.com/Pstsrppt/PortfolioPstsrppt.github.io

---

## 1. ใช้อะไรทำ?

| ประเภท | เครื่องมือ | เหตุผลที่เลือก |
|---|---|---|
| AI / Vibe Coding | Claude Code | คุยกันแบบสนทนาต่อเนื่อง แก้ไฟล์ในโปรเจกต์ได้โดยตรง ไม่ใช่แค่ตอบแชท |
| ภาษา | HTML, CSS, JavaScript ล้วน | ไม่ต้อง build เว็บไม่มี state ซับซ้อนหรือ backend ที่ต้องพึ่ง framework เปิดไฟล์ตรงได้เลย |
| 3D | Three.js (r128) | ทำโลก 3D เป็นหน้าแรกเพื่อดึงความสนใจ โหลดจาก CDN ไม่ต้องติดตั้งอะไรเพิ่ม |
| เครื่องมือเขียนโค้ด | VS Code + Live Server | แก้โค้ดแล้วหน้าเว็บรีเฟรชอัตโนมัติ |
| Hosting | GitHub Pages | ฟรี ได้ Public URL จริง และ repository เดียวกันนี้ส่งอาจารย์ตรวจได้เลย |

**ทำไมไม่ใช้ React หรือ Next.js:** เว็บพอร์ตนี้เป็นเว็บ static ล้วน ไม่มีข้อมูลที่เปลี่ยนแปลงตามผู้ใช้ ไม่มี backend/database เลือก HTML/CSS/JS ธรรมดาให้เบา deploy ง่าย และแก้ไขตรงจุดได้ทันทีโดยไม่ต้องผ่านขั้นตอน build

---

## 2. เริ่มจากอะไร?

เริ่มทำงานต่อจากเว็บที่มีโครงหน้าเปล่าและระบบข้อมูลกลาง (`data.js`) วางไว้แล้ว ตามแนวทาง Path A ของโจทย์ — คือมีผลงานจริงอยู่แล้ว 2 ชิ้น (**CakeShop** งานเดี่ยวในรายวิชา และ **POLIS** โปรเจกต์ส่วนตัว) แล้วเอาข้อมูลจริงมาเติมเต็มทีละจุด: ประวัติ ทักษะ ภาพหน้าจอ ลิงก์ GitHub/LinkedIn ไปจนถึงรายละเอียดปัญหา-ทางแก้ของแต่ละโปรเจกต์ที่ยังเป็น placeholder อยู่

> **หมายเหตุความโปร่งใส:** ช่วงเริ่มต้นสุดที่วางโครง 4 หน้าและโลก 3D ครั้งแรกเกิดขึ้นก่อนบทสนทนาที่บันทึกไว้นี้ เอกสารฉบับนี้จึงเน้นเล่ากระบวนการ "ทำต่อ-แก้ไข-เติมเนื้อหาจริง" ที่มีหลักฐานอ้างอิงกลับไปโค้ดจริงได้ทุกจุด

---

## 3. Prompt สำคัญคืออะไร?

**Prompt แรกที่เริ่มบทสนทนานี้** ไม่ใช่การขอสร้างเว็บใหม่ แต่เป็นการรีวิวของเดิม พร้อมแนบภาพหน้าจอ:

> "เอาอันที่ไม่ไปตามเมาส์ออกให้หน่อยครับ"

(หมายถึงแสงเรืองที่ตามเมาส์ในส่วน hero มีจุดหนึ่งที่ไม่ขยับตามจริง — ต้องดูโค้ด CSS แล้วแยกให้ออกว่าจุดไหนเป็น `.mglow` ที่ตามเมาส์จริง จุดไหนเป็นพื้นหลังตายตัว)

**Prompt ที่ทำให้เกิดการเปลี่ยนแปลงมากที่สุด** คือช่วงที่ขอให้ตรวจสอบทั้งเว็บ:

> "ตอนนี้ขาดอะไรบ้างครับ"

ทำให้ต้องไล่สแกนทั้ง 4 หน้าหา placeholder ที่เหลือ (`[เขียน 1-2 ประโยค]`, `ใส่ภาพ...ตรงนี้` ฯลฯ) แล้วจัดเป็นรายการงานที่เหลือ ก่อนพากลับมาทำทีละจุดตามที่ขอ:

> "เดี๋ยวผมให้ทำทีละอย่าง..."

**Prompt ที่ให้ context หนาแน่นที่สุด** คือตอนแนบเอกสาร debug log จริงของ CakeShop และ POLIS มาให้ พร้อมขอ:

> "ช่วยแก้ปัญหาพวกนี้ทีครับ หาปัญหาที่หนักที่สุดแล้วนำมาใส่แค่อันเดียวพอ ทั้งเค้กช็อปและ POLIS"

ต้องอ่านเอกสารยาวทั้งสองไฟล์ ประเมินว่าปัญหาข้อไหน "หนักที่สุด" (ตรงกับเกณฑ์ No Secret in Code ของโจทย์ และเป็นบทเรียนที่ทางเอกสารเองก็ยกเป็นอันดับ 1) แล้วเลือกมาเขียนใหม่ให้กระชับ

---

## 4. AI ช่วยอะไร?

- **ตรวจสอบทั้งเว็บ:** สแกนหา placeholder/เนื้อหาที่ยังไม่ครบ แล้วสรุปเป็นรายการงานที่เหลือ เทียบกับเกณฑ์ Non-Negotiable ของโจทย์ทีละข้อ
- **แก้บั๊ก UI/UX:** เช่น แสงตามเมาส์ที่ผิดจุด, ภาพสกรีนช็อตถูกครอปแบบสุ่มเพราะกรอบไม่มีสัดส่วนคงที่, กล้องในโลก 3D ซูมเข้าใกล้เกินไปตอนกดปุ่มถอยกลับ
- **ปรับเนื้อหาจากข้อมูลจริง:** เอาข้อมูลที่ผู้ใช้ให้มา (สกรีนช็อตโปรเจกต์, เอกสาร debug log, ลิงก์ LinkedIn, วันเกิด) มาเขียนเป็นข้อความที่กระชับและอ่านง่ายขึ้น โดยไม่เติมข้อมูลที่ไม่มีหลักฐานรองรับ
- **ตั้งค่า Git และ Deploy:** สร้าง repository ในเครื่อง, commit, push ขึ้น GitHub, แนะนำขั้นตอนเปิด GitHub Pages ทีละสเต็ป แล้วตรวจสอบว่า URL ที่ deploy แล้วเปิดได้จริง

---

## 5. เจอปัญหาอะไร?

**ปัญหาที่ 1: ปุ่ม "ถอยกลับ" ที่เพิ่มใหม่กลับทำให้กล้องซูมเข้าใกล้ยิ่งกว่าเดิม**

อาการ: ในโลก 3D ตอนซูมเข้าไปดูโปรเจกต์ใกล้ๆ แล้วกดปุ่มถอยกลับที่เพิ่งเพิ่มไป กล้องกลับซูมเข้าไปติดขนาดวัตถุเล็กๆ จนเต็มจอ แทนที่จะถอยออกมา

สาเหตุ: ฟังก์ชันวางวัตถุในโลก 3D (`place()`) มี fallback logic ที่ทำให้วัตถุใดๆ ที่มีตัวอักษร (texture) กลายเป็น "คลิกแล้วซูมเข้าไปดูใกล้" โดยอัตโนมัติ แม้จะไม่ได้ตั้งใจให้เป็นแบบนั้น จุดบอกหน้า (pagination dots) ที่ไม่มีข้อความให้อ่านเลยก็ยังคลิกซูมเข้าไปได้ด้วย

วิธีแก้: เพิ่ม option `action:false` ให้ปิดการคลิกได้แบบชัดเจน แทนที่จะปล่อยให้ fallback ตัดสินใจเอง แล้วระบุปิดเฉพาะจุดที่ไม่ควรคลิกได้

**ปัญหาที่ 2: Export เรซูเม่เป็น PDF แล้วล้นไป 2 หน้า**

อาการ: กดปุ่ม "บันทึกเป็น PDF" ที่หน้าเรซูเม่ เนื้อหาล้นไปหน้าที่สองแค่บรรทัดเดียว พร้อมกล่องพื้นหลังสีเข้มแตกครึ่งไปด้วย

สาเหตุ: CSS ที่ใช้แสดงผลบนจอปกติมีระยะห่าง/ขนาดตัวอักษรที่เผื่อไว้เกินความจำเป็นสำหรับกระดาษ A4 และยังไม่มีการปรับแต่งเฉพาะตอนพิมพ์เลย

วิธีแก้: เพิ่ม `@media print` เฉพาะสำหรับบีบ padding, margin, font-size เฉพาะตอนพิมพ์เท่านั้น ไม่กระทบมุมมองบนจอปกติ

**ปัญหาที่ 3: ไฟล์ `resume.pdf` ใช้งานได้ในเครื่องแต่จะพังตอน deploy จริง**

อาการ: ไฟล์ที่บันทึกจาก Windows ชื่อว่า `Resume.pdf` (R ใหญ่) แต่โค้ดอ้างอิงเป็น `resume.pdf` (r เล็ก)

สาเหตุ: Windows เป็นระบบไฟล์ที่ไม่สนตัวพิมพ์เล็ก-ใหญ่ จึงใช้งานได้ปกติตอน dev แต่ GitHub Pages รันบน Linux ซึ่งสนตัวพิมพ์เล็ก-ใหญ่ ลิงก์จะเสีย (404) ทันทีที่ deploy จริง

วิธีแก้: เปลี่ยนชื่อไฟล์ให้ตรงกับที่โค้ดอ้างอิงก่อน push ขึ้น GitHub — เป็นตัวอย่างจริงว่าทำไมต้องทดสอบบน production environment ไม่ใช่แค่เครื่องตัวเอง

---

## 6. ยากตรงไหน?

| ประเด็น | วิธีแก้ |
|---|---|
| บั๊กที่ไม่มี error เตือนเลย | จุดบอกหน้าในโลก 3D ที่คลิกไม่ควรได้ไม่มี error ใดๆ ขึ้นมาเลย ต้องไล่อ่าน state การซูม (peek/focus) ทั้งระบบก่อนถึงเจอสาเหตุจริง |
| ภาพสกรีนช็อตถูกครอปไม่แน่นอน | กรอบใส่ภาพยืดความสูงตามเนื้อหาข้างๆ (CSS Grid) ทำให้ครอปภาพไม่เหมือนกันทุกโปรเจกต์ ต้องเปลี่ยนไปกำหนด `aspect-ratio` คงที่แทนเพื่อให้ครอปสม่ำเสมอ |
| ข้อมูลตัวตนต้องแม่นเป๊ะ | ก่อนใส่ชื่อภาษาไทย วันเกิด หรือแก้ URL LinkedIn ลงเว็บจริง ต้องหยุดถามยืนยันกับผู้ใช้ทุกครั้ง เพราะเป็นข้อมูลตัวตนที่ผิดพลาดไม่ได้เลย ต่างจากงาน UI ทั่วไปที่แก้ทีหลังได้ |
| แยกแยะปัญหาคนละบริบท | ตอนผู้ใช้ส่งเอกสาร debug log ของ CakeShop/POLIS มา ต้องระวังไม่เอา "ปัญหาที่เจอตอนพัฒนาโปรเจกต์เหล่านั้น" มาปนกับ "ปัญหาที่เจอตอน Vibe Coding เว็บพอร์ตนี้" เพราะเป็นคนละบริบทกัน (อันหนึ่งเป็นเนื้อหาที่นำไปแสดงในเว็บ อีกอันเป็นกระบวนการสร้างเว็บพอร์ตเอง) |

---

## 7. AI ทำผิดตรงไหน?

**ครั้งที่ 1:** ตอนผู้ใช้ขอปุ่ม "ดูผลงานได้เลย" ให้การ์ดข้อมูลส่วนตัว AI ใส่ปุ่มตามตัวอักษรของคำขอ แต่ตำแหน่งไม่ตรงกับที่ผู้ใช้คิดไว้ในหัว ต้องขอให้เอาออกแล้วย้ายไปไว้ข้างๆ ป้าย "ดูได้ใน" แทน — สะท้อนว่า AI ตีความคำสั่งตามตัวอักษรได้ถูก แต่ไม่ได้อ่านใจว่าตำแหน่งที่ผู้ใช้นึกไว้คือจุดไหน

**ครั้งที่ 2:** ตอนใส่ไอคอน GitHub/LinkedIn ครั้งแรกในปุ่ม CTA ของ hero AI เลือกความโปร่งใสของสี/กรอบอ่อนเกินไปจนกลืนไปกับพื้นหลังเข้ม ผู้ใช้เปิดเว็บจริงแล้วมองไม่เห็นเลย ต้องแก้ contrast เพิ่มอีกรอบ — เป็นตัวอย่างว่าโค้ดถูกต้อง (element มีอยู่จริง) แต่ใช้งานจริงไม่ได้เพราะมองไม่เห็น

**ครั้งที่ 3 (สำคัญที่สุด):** ตอนแก้บั๊กปุ่มถอยกลับในโลก 3D AI เข้าใจผิดในตอนแรกว่าปัญหาน่าจะอยู่ที่ตัวปุ่มเอง ทั้งที่จริงสาเหตุซ่อนอยู่คนละจุด (fallback logic ของฟังก์ชันวางวัตถุที่เขียนไว้ก่อนหน้า) ต้องไล่อ่านโค้ดทั้งระบบก่อนถึงเจอจุดจริง — แสดงว่าการเดาสาเหตุจาก "อาการ" อย่างเดียวโดยไม่ไล่โค้ดจริงเสี่ยงผิดทางได้

---

## 8. คุณปรับเองตรงไหน?

- เลือกเองว่าจะใช้ภาพหน้าจอไหนเป็นภาพหลักของแต่ละโปรเจกต์ จากภาพหลายภาพที่มีอยู่
- ตัดสินใจแยก CV เป็นไฟล์ PDF ต่างหาก แทนที่จะใช้แค่หน้าเว็บอย่างเดียวตามที่เป็นค่าเริ่มต้น
- ปรับใจว่าให้ลิงก์ GitHub/LinkedIn ย้ายไปรวมกับอีเมลในการ์ดข้อมูลส่วนตัว แทนปุ่มไอคอนแยกที่ AI เสนอมาให้ก่อนหน้า เพราะอยากให้ข้อมูลติดต่อทั้งหมดอยู่จุดเดียว
- ยืนยัน/แก้ไขข้อมูลส่วนตัวทุกจุดเอง (คำสะกดชื่อภาษาไทย, วันเกิด, URL LinkedIn ที่สะกดคลาดเคลื่อนไปตัวหนึ่ง) ก่อนเผยแพร่จริง
- เลือกเองว่าปัญหาข้อไหนจากเอกสาร debug log "หนักที่สุด" ที่ควรนำมาเล่าในโปรเจกต์

---

## 9. ทำไม Design แบบนี้?

- ใช้พื้นหลังเข้ม (dark navy) สลับกับพื้นขาวเป็นช่วงๆ ทั่วเว็บ เพื่อแบ่งจังหวะการอ่านและดึงจุดสำคัญ (hero, การ์ดเป้าหมาย) ให้เด่นออกมา แทนที่จะให้ทั้งเว็บเป็นสีเดียวแบนๆ
- ให้โลก 3D เป็นหน้าแรกเพื่อดึงความสนใจตั้งแต่วินาทีแรก ส่วนหน้า about/projects/resume เป็นเวอร์ชัน "อ่านเร็ว" คู่ขนานสำหรับคนที่อยากได้ข้อมูลตรงๆ ไม่อยากเล่น 3D — ครอบคลุมทั้ง recruiter ที่รีบและคนที่อยากดูอะไรที่แตกต่าง
- แต่ละโปรเจกต์มีสีประจำตัว (ชมพูสำหรับ CakeShop, ทองสำหรับ POLIS) ใช้สอดคล้องกันทั้งในโลก 3D และหน้า projects.html เพื่อให้จำแต่ละโปรเจกต์ได้ง่ายและรู้สึกว่าเป็นระบบเดียวกัน

---

## 10. ทำใหม่จะปรับอะไร?

1. **เพิ่ม favicon และ Open Graph meta tags** ที่ยังไม่มีเลยทั้ง 4 หน้า เพื่อให้แชร์ลิงก์ไปที่อื่นแล้วมีไอคอนแท็บและรูปพรีวิวที่ถูกต้อง
2. **ทดสอบ responsive บนมือถือจริง** ตั้งแต่ต้น แทนที่จะรอมาทดสอบตอนใกล้ส่งงาน จะได้จับปัญหาการจัดวางได้เร็วกว่านี้
3. **เมื่อมีโปรเจกต์ AI/RAG จริง** จะนำมาแทนที่ป้าย "กำลังเรียนรู้" ในหน้าทักษะด้วยผลงานจริงที่พิสูจน์ทักษะนั้นได้

---

## สรุปสิ่งที่ได้เรียนรู้จากการใช้ AI

บทเรียนสำคัญที่สุดคือ **AI สร้างให้เร็วได้ แต่ต้องตรวจทุกจุดเอง** — ไม่ว่าจะเป็นเรื่องข้อมูลตัวตนที่ต้องยืนยันความถูกต้องก่อนเผยแพร่ทุกครั้ง เรื่องบั๊กที่ AI เข้าใจสาเหตุผิดในตอนแรกแล้วต้องไล่โค้ดจริงถึงจะเจอ หรือเรื่องดีไซน์ที่ AI เลือกค่าที่ "โค้ดถูกแต่ใช้งานจริงไม่ได้" (เช่น สีจางเกินจนมองไม่เห็น) — จุดร่วมของทุกบทเรียนคือ **ต้องเปิดดูผลลัพธ์จริงเสมอ** ไม่ใช่เชื่อว่าโค้ดที่ AI เขียนถูกต้องเพียงเพราะไม่มี error ขึ้นมา
